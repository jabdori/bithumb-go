# 연결 관리

## 개요

빗썸 WebSocket API의 연결을 안정적으로 관리하고 유지하는 방법에 대한 설명입니다.

## Connection 관리

### PING/PONG

빗썸 API WebSocket 서버는 커넥션을 안정적으로 관리/유지하기 위해 WebSocket PING/PONG Frame을 제공합니다.

참고 문서: [RFC 6455 - WebSocket Protocol](https://tools.ietf.org/html/rfc6455#section-5.5.2)

### Client to Server PING

WebSocket 연결을 유지하기 위해 클라이언트에서 서버로 PING 메시지를 전송할 수 있습니다.

#### 주요 사항

1. **Idle Timeout**: 기본적으로 서버는 아무런 데이터도 수신, 발신 되지 않은 채 약 **120초**가 경과하면 Idle Timeout으로 WebSocket Connection을 종료합니다.

2. **연결 유지**: 이를 방지하기 위해 클라이언트에서 서버로 PING 메시지를 보내서 Connection을 유지하고, WebSocket 서버의 상태와 WebSocket Connection Status를 파악할 수 있습니다.

3. **PING Frame 지원**: 빗썸 API WebSocket 서버에서는 PING Frame 수신 대응이 준비되어 있습니다. 간단한 구현으로 클라이언트에서 PING 요청/PONG 응답(PING에 대한 응답 Frame)을 통해 서버 상태를 파악할 수 있습니다.

4. **클라이언트 라이브러리**: 이에 대한 구성은 해당 클라이언트 개발 문서를 확인하시기 바랍니다. (대부분의 라이브러리는 ping 함수가 내장되어 있을 가능성이 높습니다.)

5. **상태 확인 응답**: Connection이 유지되고 있으면 `{"status":"UP"}` 응답을 **10초 간격**으로 받을 수 있습니다.

#### 예제

```bash
$ wscat -c wss://ws-api.bithumb.com/websocket/v1
Connected (press CTRL+C to quit)
< PING
< {"status":"UP"}
< {"status":"UP"}
< {"status":"UP"}
...
```

## 연결 관리 권장사항

### 1. PING/PONG 메시지 구현

- **주기**: 최소 60초 간격으로 PING 메시지 전송 권장 (Idle Timeout 120초의 절반)
- **응답 확인**: PONG 응답을 받지 못할 경우 재연결 로직 실행
- **상태 모니터링**: `{"status":"UP"}` 메시지를 통해 서버 상태 확인

### 2. 재연결 전략

```javascript
// 예제: 재연결 로직 구현
let reconnectAttempts = 0;
const maxReconnectAttempts = 5;
const reconnectDelay = 3000; // 3초

function connect() {
  ws = new WebSocket('wss://ws-api.bithumb.com/websocket/v1');

  ws.onopen = () => {
    console.log('WebSocket connected');
    reconnectAttempts = 0;
    // PING 타이머 시작
    startPingTimer();
  };

  ws.onclose = () => {
    console.log('WebSocket disconnected');
    if (reconnectAttempts < maxReconnectAttempts) {
      setTimeout(() => {
        reconnectAttempts++;
        connect();
      }, reconnectDelay * reconnectAttempts);
    }
  };

  ws.onerror = (error) => {
    console.error('WebSocket error:', error);
  };
}

function startPingTimer() {
  setInterval(() => {
    if (ws.readyState === WebSocket.OPEN) {
      ws.ping();
    }
  }, 60000); // 60초마다 PING
}
```

### 3. 연결 상태 모니터링

- **연결 상태**: `ws.readyState`를 주기적으로 확인
- **하트비at**: 서버로부터 `{"status":"UP"}` 메시지 수신 확인
- **네트워크 변경**: 네트워크 변경 감지 시 재연결 시도

### 4. 에러 처리

- 연결 실패시 적절한 에러 메시지 사용자에게 표시
- 일시적인 �트워크 오류인 경우 자동 재연결
- 연결 timeout 설정 (예: 30초)

## 참고

- 최종 업데이트: 10일 전
- API 버전: v2.1.5
- WebSocket Endpoint: `wss://ws-api.bithumb.com/websocket/v1`
- Idle Timeout: 120초
- Status Check 간격: 10초
- [원본 문서](https://apidocs.bithumb.com/v2.1.5/reference/연결-관리)
